# 服务器配置
server.port=8080
server.servlet.context-path=/

# 数据库配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://124.223.76.88:3306/oxford_vocabulary?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=henry
spring.datasource.password=henryfang

# 连接池配置
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# MyBatis配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.oxford.vocabulary.entity
mybatis.configuration.map-underscore-to-camel-case=true

# 日志配置
logging.level.root=INFO
logging.level.com.oxford.vocabulary=DEBUG
logging.level.org.springframework.web=DEBUG

# 缓存配置
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=300s

# 跨域配置
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=* 